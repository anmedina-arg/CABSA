/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',
  // reactStrictMode: true,
  // distDir: 'dist',
  // trailingSlash: true,
  // assetPrefix: '.',
};

module.exports = nextConfig;
